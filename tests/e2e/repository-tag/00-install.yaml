apiVersion: security.brcmlabs.com/v1
kind: Repository
metadata:
  name: l7-gw-myframework-tag
spec:
  name: l7-gw-myframework-tag
  enabled: true
  endpoint: $TESTREPO_URL
  tag: test1
  auth:
    vendor: Github
    existingSecretName: test-repository-secret